import { LandingPageBackgroundHeaderDirective } from './landing-page-background-header.directive';

describe('LandingPageBackgroundHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new LandingPageBackgroundHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
